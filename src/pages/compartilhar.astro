---
import Base from '../layouts/Base.astro';

const siteUrl = import.meta.env.SITE_URL || 'https://empadas-da-simone.vercel.app';
---

<Base
  title="Seu QR Code — Empadas da Simone"
  description="QR Code do cardápio das empadas da Simone. Imprima, compartilhe ou baixe."
>
  <main class="max-w-2xl mx-auto px-4 sm:px-6 py-12 sm:py-20 text-center">
    <a
      href="/"
      class="inline-flex items-center gap-2 text-muted hover:text-primary text-sm font-semibold mb-10 transition"
    >
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-4 h-4">
        <path d="m12 19-7-7 7-7" />
        <path d="M19 12H5" />
      </svg>
      Voltar ao cardápio
    </a>

    <p class="inline-block font-body text-xs sm:text-sm uppercase tracking-[0.3em] text-primary mb-4">
      Para imprimir ou compartilhar
    </p>
    <h1 class="font-display text-4xl sm:text-5xl text-fg leading-tight mb-4">
      Seu <span class="italic text-primary">QR Code</span>
    </h1>
    <p class="text-muted text-base sm:text-lg max-w-md mx-auto mb-10">
      Aponte a câmera para abrir o cardápio.
    </p>

    <div class="inline-block bg-surface p-7 sm:p-9 rounded-3xl shadow-md border border-border mb-8">
      <div
        id="qrcode"
        data-url={siteUrl}
        class="mx-auto [&_canvas]:rounded-lg [&_img]:rounded-lg"
      ></div>
    </div>

    <div class="mb-8">
      <p class="font-body text-xs uppercase tracking-widest text-subtle mb-2">
        Link
      </p>
      <a
        href={siteUrl}
        class="font-display text-primary hover:text-fg underline underline-offset-4 decoration-primary-soft break-all"
      >
        {siteUrl}
      </a>
    </div>

    <div class="flex flex-col sm:flex-row gap-3 justify-center">
      <button
        type="button"
        id="download-btn"
        class="inline-flex items-center justify-center gap-2 bg-primary text-primary-fg font-semibold px-6 py-3 rounded-full shadow-sm hover:shadow hover:brightness-90 transition"
      >
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-5 h-5">
          <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
          <polyline points="7 10 12 15 17 10" />
          <line x1="12" x2="12" y1="15" y2="3" />
        </svg>
        Baixar como PNG
      </button>

      <a
        href="/"
        class="inline-flex items-center justify-center gap-2 bg-primary-soft text-primary font-semibold px-6 py-3 rounded-full hover:brightness-95 transition"
      >
        Ver o cardápio
      </a>
    </div>

    <div id="download-status" class="mt-6 text-sm text-success hidden">
      QR Code baixado!
    </div>
  </main>
</Base>

<script is:inline>
  (function () {
    const container = document.getElementById('qrcode');
    if (!container) return;
    const url = container.getAttribute('data-url') || '';

    const renderQR = () => {
      container.innerHTML = '';
      // eslint-disable-next-line no-undef
      new QRCode(container, {
        text: url,
        width: 256,
        height: 256,
        colorDark: '#1e1e2e',
        colorLight: '#ffffff',
        correctLevel: QRCode.CorrectLevel.M,
      });
    };

    const s = document.createElement('script');
    s.src = 'https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js';
    s.async = true;
    s.onload = renderQR;
    s.onerror = () => {
      container.textContent = 'Não foi possível carregar o gerador de QR Code.';
    };
    document.head.appendChild(s);

    const downloadBtn = document.getElementById('download-btn');
    const status = document.getElementById('download-status');

    const tryDownload = (attempts = 0) => {
      const canvas = container.querySelector('canvas');
      if (canvas) {
        try {
          const dataUrl = canvas.toDataURL('image/png');
          const a = document.createElement('a');
          a.href = dataUrl;
          a.download = 'empadas-da-simone-qrcode.png';
          document.body.appendChild(a);
          a.click();
          document.body.removeChild(a);
          if (status) {
            status.classList.remove('hidden');
            setTimeout(() => status.classList.add('hidden'), 2400);
          }
        } catch (err) {
          console.error('Erro ao gerar PNG:', err);
        }
        return;
      }
      if (attempts < 30) {
        setTimeout(() => tryDownload(attempts + 1), 150);
      }
    };

    downloadBtn?.addEventListener('click', () => tryDownload());
  })();
</script>
